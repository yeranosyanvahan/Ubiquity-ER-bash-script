#!/bin/bash

#INPUT
read description < ./input.txt
cat ./input.txt | tail -1 > ./tmp/tmp.txt
read ip < ./tmp/tmp.txt

set="/opt/vyatta/sbin/vyatta-cfg-cmd-wrapper"



$set begin
$set delete service suspend user-ip $ip
$set commit
$set discard
$set end

